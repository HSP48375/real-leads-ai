# =============================================================================
# LOVABLE CLOUD / SUPABASE CONFIGURATION
# =============================================================================
# These are automatically provided by Lovable Cloud and should not be changed
# For local development, get these from your Lovable Cloud dashboard

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key-here
VITE_SUPABASE_PROJECT_ID=your-project-id

# =============================================================================
# EDGE FUNCTION SECRETS (Server-side only - stored in Supabase Vault)
# =============================================================================
# These are NOT exposed to the client and must be added via Lovable Cloud UI
# Go to: Lovable Dashboard → Your Project → Settings → Secrets

# Supabase Service Role Key (full admin access - handle with extreme care)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Supabase Database Connection String
SUPABASE_DB_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# Supabase Keys (duplicates for edge function access)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_PUBLISHABLE_KEY=your-anon-key-here

# =============================================================================
# THIRD-PARTY API KEYS
# =============================================================================

# Stripe (Payment Processing)
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_TEST_SECRET_KEY=sk_test_...
STRIPE_TEST_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET_TEST=whsec_...

# Apify (Web Scraping - FSBO.com actor)
# Get from: https://console.apify.com/account/integrations
APIFY_API_KEY=apify_api_...

# Olostep (Deep Scraping - Contact Info Extraction)
# Get from: https://olostep.com/dashboard
OLOSTEP_API_KEY=your-olostep-key

# ZenRows (Backup Scraping Service - Optional)
# Get from: https://www.zenrows.com/
ZENROWS_API_KEY=your-zenrows-key

# Mapbox (Geocoding & City Discovery)
# Get from: https://account.mapbox.com/access-tokens/
MAPBOX_API_KEY=pk.your-mapbox-key

# Resend (Email Delivery)
# Get from: https://resend.com/api-keys
RESEND_API_KEY=re_...

# Google Cloud (Google Sheets API for lead delivery)
# Get from: https://console.cloud.google.com/apis/credentials
# Must be a service account JSON key (entire JSON as single-line string)
GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}
GOOGLE_SHEETS_FOLDER_ID=your-google-drive-folder-id

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Base URL for the application (used in emails and webhooks)
APP_BASE_URL=https://your-app.lovableproject.com

# Make.com Webhook URL (Optional - for external automation)
MAKE_WEBHOOK_URL=https://hook.us1.make.com/...

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================
# 
# 1. NEVER commit the actual .env file to Git
# 2. All secrets must be added via Lovable Cloud UI (Settings → Secrets)
# 3. Frontend env vars (VITE_*) are exposed to the client - never put secrets there
# 4. Edge function secrets are server-side only and accessed via Deno.env.get()
# 5. When deploying outside Lovable, configure these in your hosting environment
#
# =============================================================================
# COST OPTIMIZATION SETTINGS
# =============================================================================
#
# Current Apify Costs (approximate):
# - FSBO city search: ~$0.05 per city
# - Deep scraping: ~$0.02 per listing (via Olostep)
#
# Tier-Based City Limits (to keep costs <2% of revenue):
# - Starter ($97): 2 cities max
# - Growth ($197): 5 cities max
# - Pro ($397): 10 cities max
# - Enterprise ($697): 20 cities max
#
# Target Lead Quotas:
# - Starter: 20-25 leads
# - Growth: 40-50 leads
# - Pro: 110-130 leads
# - Enterprise: 150-200 leads
#
# =============================================================================
